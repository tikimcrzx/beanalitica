"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
const safe_1 = require("colors/safe");
const winston_1 = require("winston");
const nestLikeConsoleFormat = () => winston_1.format.printf((_a) => {
    var { context, level, timestamp, message } = _a, meta = __rest(_a, ["context", "level", "timestamp", "message"]);
    return `${safe_1.green('[NestWinston]')} ` +
        `${safe_1.yellow(level.charAt(0).toUpperCase() + level.slice(1))}\t` +
        ('undefined' !== typeof timestamp ? `${new Date(timestamp).toLocaleString()} ` : '') +
        ('undefined' !== typeof context ? `${safe_1.yellow('[' + context + ']')} ` : '') +
        `${safe_1.green(message)} - ` +
        `${JSON.stringify(meta)}`;
});
exports.utilities = {
    format: {
        nestLike: nestLikeConsoleFormat,
    },
};
